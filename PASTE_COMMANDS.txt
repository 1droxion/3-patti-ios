3 PATTI SOCIAL V1.4 — INSTALL

cd /workspaces/3-patti-ios
unzip -o three_patti_v14_persistent_backend.zip
rm three_patti_v14_persistent_backend.zip
flutter pub get
git add .
git commit -m "V1.4 persistent backend wallet auth ledger"
git push

VERSION EXPECTED:
1.4.0+15

AFTER ADDING SUPABASE + AUTH ENV VARS TO VERCEL:
https://3-patti-ios.vercel.app/health

Expected persistence fields:
mode: supabase-postgres
persistent: true
supabaseConfigured: true

Keep CASH_MODE_ENABLED=false until external approvals are complete.
