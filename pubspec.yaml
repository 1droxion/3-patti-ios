name: three_patti_usa
publish_to: 'none'
description: 3 Patti Social premium full-screen iOS lobby and multiplayer prototype.
version: 0.9.0+10

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/sfx/
