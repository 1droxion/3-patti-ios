name: three_patti_usa
publish_to: 'none'
description: USA-first social Teen Patti MVP with server-backed multiplayer.
version: 0.2.0+2

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
